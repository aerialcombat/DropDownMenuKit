import XCTest
@testable import DropDownMenuKit_master

final class DropDownMenuKit_masterTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DropDownMenuKit_master().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
